<UserControl x:Class="Controls.CMButton"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ClubManager.Controls"
        mc:Ignorable="d"
        d:DesignHeight="64" d:DesignWidth="64" KeyboardNavigation.IsTabStop="True">

    <Grid Height="Auto" Width="Auto" >
        <Border Name="MiBorde" BorderThickness="1" Height="Auto" Width="Auto" BorderBrush="#D2D5D6" Margin="0,0,0,0" CornerRadius="5" ForceCursor="True" Cursor="Hand">
            <Grid Height="Auto" Width="Auto">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Image Grid.Row="0" RenderOptions.BitmapScalingMode="Fant" Height="Auto" Width="Auto" HorizontalAlignment="Center" Margin="0,1,0,0" Name="ButtonImage" Stretch="Fill" VerticalAlignment="Center" ForceCursor="True" Cursor="Hand"/>
                    <Label Grid.Row="0" HorizontalAlignment="Center" FontWeight="Bold" VerticalAlignment="Center" Height="Auto" Name="LBL_Text" HorizontalContentAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
